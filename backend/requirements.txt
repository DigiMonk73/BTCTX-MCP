# Core framework
fastapi==0.141.1
pydantic==2.13.5
starlette==1.7.0
uvicorn==0.53.0

# Database
sqlalchemy==2.0.54
alembic==1.20.0  # schema migrations (backend/migrations/, run at startup)

# Authentication
bcrypt==5.0.0

# HTTP & networking
httpx==0.28.1
requests==2.34.2
python-multipart==0.0.32

# Utilities
python-dotenv==1.2.3
python-dateutil==2.9.0.post0
itsdangerous==2.2.0
tzdata==2026.4  # timezone rules for the tax timezone (bundled, OS-independent)

# Encryption (for backups)
cryptography==50.0.1

# PDF generation & manipulation
pypdf==6.19.0
reportlab==4.4.10
