# Hardening findings (v0.9.2)

Every finding from the hardening plan (`docs/HARDENING_AND_REDESIGN_PLAN.md`),
Phases 1 and 2. Verdicts: **bug** (fix with a test that fails on the old code),
**tax** (a bug whose fix changes tax figures for existing data: owner's OK
first), **ok** (checked, no change), **deferred** (owner's OK recorded).

| ID | What | Where | Evidence | Verdict | Fix / test |
|---|---|---|---|---|---|
| F1 | A "Spent" BTC withdrawal entered in the form with blank proceeds is saved with $0 proceeds, a loss of its whole basis, instead of its market value at the day's price (the rule for unpriced spends). The form sends `proceeds_usd: 0`, which the backend reads as "priced at $0"; only imports send "missing". The form warns but saves. | `utils/transactionForm.ts` `buildTransactionPayload`; `services/transaction.py` `_withdrawal_gross_proceeds` | e2e `create.e2e.ts` "Spent without proceeds…" (marked `test.fail`) | tax (new entries only; no recalculation) | fixed: the form sends null for a blank; blank defaults; `cli review` lists old $0 spends; Vitest `transactionForm.test.ts`, e2e F1 test (was `test.fail`) |
| F2 | "Lost" withdrawals: the transaction records a capital loss of the basis (`realized_gain_usd = −basis`), but Form 8949 excludes Lost as non-taxable. Anything summing `realized_gain_usd` (dashboard, reports) may count a loss the forms don't. | `services/transaction.py:715`, `reports/form_8949.py:99` | e2e `create.e2e.ts` Lost test (locks today's behavior) | tax (**needs Recalculate Ledger**) | fixed per owner (2026-09-26): Lost has $0 gain like Gift; `test_stress_and_forms.py::test_edge_withdrawal_lost_no_gain_or_loss` fails on the old code |
| F3 | FMV "Refresh" in the form asks for the price of the UTC date of the local time (`toISOString().split("T")[0]`), so an evening entry in the US or a morning entry in Tokyo is priced on the wrong day. | `components/TransactionForm.tsx` `handleRefreshFmv` | code reading | ok | no change: the server values by the UTC day too (`transaction.py` `_value_income_deposit`), and CoinGecko's daily price is at 00:00 UTC, so the UTC date is the nearest daily price; the form and server agree |
| F4 | The BTC-transfer fee's USD hint uses a hardcoded $30,000 price (`mockBtcPrice`). | `components/TransactionForm.tsx` | code reading | bug (display only) | fixed: the made-up estimate is gone; e2e transfer test |
| F5 | The form sends 0 for every numeric field it doesn't use (`cost_basis_usd` on a Transfer, `fmv_usd` on a Spent withdrawal), so saving a row created by an import or the API turns `null` into `0`. Harmless where 0 and null mean the same; it's the same root cause as F1 where they don't. | `utils/transactionForm.ts` | e2e `edit.e2e.ts` | ok after F1 | fields a type doesn't use are sent as null; where null and 0 differ (F1, income basis) null now means "not given" |
| F6 | The sats converter rounds BTC to 5 decimals (1,000 sats): $1 at $60,000 shows 0.00002 BTC next to 1666 sats. | `components/BtcConverter.tsx` `round` | code reading | bug (display only) | fixed: 8 decimals, plain notation; e2e widgets test |
| F7 | Mac app silently took a random port after a quick relaunch (owner report, 2026-09-25): the port probe bound without `SO_REUSEADDR`, so TIME_WAIT connections from the previous run made it fail; no retry, no log file. | `desktop/entrypoint.py` `find_free_port` | `test_desktop_ports.py::test_port_in_time_wait_is_still_ours` (the old probe fails, the new bind succeeds) | bug | fixed: bind with SO_REUSEADDR + retries, single instance, dialog, log file |
| F8 | `POST /api/backup/download` and `/api/backup/restore` skipped the login-only check their router documents, so an `API_KEY` client could copy or replace the database. | `routers/backup.py` | `test_security.py::test_api_key_cannot_download_or_restore_backups` (fails on the old code) | bug (security) | fixed |
| F9 | The Mac app's MCP setup put the user's real password in plain-text AI configs (owner report, 2026-09-25). | `utils/aiSetup.ts`, `mcp_server/client.py` | owner report | bug (security) | fixed: local key file (`mcp.json`, 0600), bearer auth from localhost only; tests in `mcp_server/tests/test_key_file.py`, e2e `settings.e2e.ts` |
| F10 | CSV import reads a date without a timezone as UTC, and a date-only value as UTC midnight: "2024-01-01" lands on Dec 31, 2023 in a US tax timezone (wrong tax year, holding period off by a day). The MCP entry import already uses the tax timezone and noon. | `services/csv_import.py` `_parse_date` | sweep probe | bug (new imports only) | open |
| F11 | Price lookups refused today's UTC date as "the future" on a machine west of UTC (the Mac app): every evening an income deposit with a blank basis got a 422 and FMV Refresh failed. | `services/bitcoin.py` `get_historical_price` | `test_price_dates.py` (fails on the old code) | bug | fixed |
| F12 | A BTC withdrawal with no purpose (or "N/A", or a typo) gets $0 proceeds, so its whole basis becomes a loss on Form 8949. The UI requires a purpose; the API, CSV and MCP don't. | `services/transaction.py` `_withdrawal_gross_proceeds`, `csv_import.py`, `schemas/transaction.py` | sweep probe | bug | fixed: a BTC withdrawal needs Spent/Gift/Donation/Lost (any case, stored canonically); `test_input_validation.py` |
| F13 | Form 8949 leaves out Gift/Donation/Lost by exact, case-sensitive match, while the ledger lower-cases: a "gift" from the API/CSV/MCP gets $0 proceeds and gain but still prints on 8949 with its basis (proceeds − cost ≠ gain on the form). | `reports/form_8949.py` | sweep probe | bug (forms only) | fixed: case-insensitive filter; `test_input_validation.py::test_lowercase_gift…` fails on the old code |
| F14 | A transfer's BTC fee (every recalculation) and an unpriced Spent withdrawal (once) are valued at **today's** price when the historical lookup fails, silently; the fee is re-priced on every save, so past gains can change and saves fail offline. | `services/transaction.py` `get_btc_price` | sweep probe (2024 fee priced at the live $60,000) | tax: **owner decision** | open |
| F15 | A BTC deposit that isn't income (MyBTC, Gift, N/A) with a blank basis gets a $0 lot: every later sale is all gain. The CSV import only warns. | `services/transaction.py` lot creation | sweep probe | tax: **owner decision** | open |
| F16 | A Sell with neither gross nor net proceeds (API/MCP) gets $0 proceeds, a loss of its basis. | `schemas/transaction.py`, `services/transaction.py` | sweep probe | bug | fixed: a Sell needs proceeds, and a fee no larger than them; `test_input_validation.py` |
| F17 | The tax report's gifts section shows $0 FMV for a gift whose FMV wasn't given or couldn't be priced. | `reports/reporting_core.py` `_build_gifts_donations_lost` | code reading | bug (display) | open |
| F18 | The complete tax report's capital-gains summary disagrees with Form 8949: it buckets whole transactions by the first lot's holding period (a sale across short- and long-term lots goes all to one), leaves out transfer-fee disposals, and adds the basis of gifts/donations/lost. | `reports/reporting_core.py` `_build_capital_gains_summary`, `_build_capital_gains_transactions_*` | sweep probe (2024: summary ST 0 vs 8949 ST 21,000 gain) | bug (report summary only; the forms are right) | open |
| F19 | River Buy: is "Sent Amount" the subtotal (before River's fee) or the total paid? The code assumes the subtotal; if it's the total, basis and the cash debit are overstated by the fee. | `services/river_import.py` | no receipt | **needs a River buy receipt** | open |
| F20 | River sends: the code debits Sent + fee. v0.7.0 reconciled to the satoshi when only Sent was debited, which suggests Sent already includes the fee, i.e. the fee is now counted twice. | `river_import.py` `ledger_amount` | history (commit 1aa1602) | **needs a River withdrawal detail** | open |
| F21 | River's Fee Currency column is ignored (forced by row type). | `river_import.py` | code reading | bug (warn on a mismatch) | open |
| F22 | River timestamps are assumed UTC without evidence. | `river_import.py` | code reading | **needs one River row near midnight with its receipt** | open |
| F23 | The CSV instructions PDF contradicts the code: Buy basis "including fees" (the code adds the fee again) and Sell proceeds as "the amount received" (the code wants gross, before the fee: the v0.9.1 River bug pattern); the Transfer/Withdrawal fee rule isn't stated. | `backend/assets/csv_import_instructions.pdf` | PDF text vs `csv_import.py` | bug (docs) | open |
| F24 | A Spent withdrawal with a BTC network fee: proceeds are reduced by the fee's share while the fee BTC's basis is also consumed; a Transfer's fee is instead a disposal at its value. $1,000 spend + fee reports $990.10 proceeds (selling-expense view: $1,000; fee-as-disposal view: $1,010). | `services/transaction.py` Spent fee offset; `test_disposal_proceeds.py` locks it | sweep worked example | tax: **owner/tax adviser decision** | open |
| F25 | CSV import rejects a USD transfer with a USD fee (it wants BTC), which the ledger requires. | `csv_import.py` | code reading | bug | open |
| F26 | Doc text: entry-import schema says "UTC when no offset" (it's the tax timezone, bare date = noon); the CSV template's Lost row says "(capital loss)". | `schemas/entry_import.py`, `csv_import.py` template | code reading | bug (docs) | open |
| F27 | Amounts: "1000000000000" BTC is saved, then the API can't serialize it and **the transaction list fails with a 500 for good**; "1E-9" is stored as 0 (exponent notation skips the decimal checks); USD amounts accept 8 decimals; no maximum. | `schemas/transaction.py` validators; service commits before response validation | audit probe | bug | fixed: decimals counted on the number, 10 integer digits, 21M BTC, cents for USD accounts; `test_input_validation.py` |
| F28 | Unknown account ids (7, 999) are accepted; ledger lines point at accounts that don't exist. | `services/transaction.py` `_enforce_transaction_type_rules` | audit probe | bug | fixed: only Bank, Wallet, Exchange USD/BTC and External; `test_input_validation.py` |
| F29 | Negative or zero amounts, and negative basis/proceeds/fees, are accepted by POST/PUT (entry import already rejects them). | `schemas/transaction.py` | audit probe | bug | fixed: amount > 0, no negative fee/basis/proceeds/FMV; `test_input_validation.py` |
| F30 | CSV and MCP entry import silently drop a malformed optional number ("1.123" basis, a 9-decimal fee) instead of rejecting the row. | `csv_import.py` `_parse_decimal` | audit probe | bug | open |
| F31 | `DELETE /api/transactions/delete_all` and the debug router accept the optional `API_KEY`. | `routers/transaction.py`, `main.py` | audit probe | bug (security) | open |
| F32 | The tax timezone (which moves tax years and holding periods) can be changed with the `API_KEY` or the AI assistant key. | `routers/settings.py` | audit probe | bug (security) | open |
| F33 | A transaction without a timestamp gives a 500. | `services/transaction.py` | audit probe | bug | fixed: timestamp required on create (422); `test_input_validation.py` |
| F34 | Fee rules: a BTC fee on a Bank withdrawal is accepted (Bank loses dollars, BTC Fees gains BTC); a withdrawal's fee may exceed its amount; a Sell's fee may exceed its proceeds. | `services/transaction.py` `_enforce_fee_rules` | audit probe | bug | fixed: a withdrawal/deposit fee must be in the account's currency; a Sell fee can't exceed its proceeds (a withdrawal fee may exceed a small amount: real on-chain case); `test_input_validation.py` |
| F35 | A partial PUT (only `to_account_id`, say) is validated on the fields sent, not the merged transaction ("Unknown transaction type: None"). | `services/transaction.py` update | audit probe | bug | fixed: an edit is validated merged with the stored row; `test_input_validation.py` |
| F36 | `DELETE /api/users/{own id}` fails with a 500 (accounts.user_id NOT NULL). | `services/user.py` | audit probe | bug | open |
| F37 | An empty username or password is accepted (an empty password silently keeps the old one); sessions stay valid after the credentials change. | `routers/user.py`, `services/user.py` | audit probe | bug (security) | open |
| F38 | `/docs`, `/redoc` and `/openapi.json` are public. | `main.py` | audit probe | bug (security, low) | open |
| F39 | Smaller: dates before 2009-01-03 or years in the future are accepted; `source`/`purpose` have no length limit; error messages show `TxType.BUY`; `/api` (no slash) returns the web page. | various | audit probe | bug (low) | open |
